<view class="page-news-detail" a:if="{{!loading && news}}">
  <image class="detail-cover" src="{{news.contentImage || news.displayImage}}" mode="widthFix" lazy-load="{{true}}" />
</view>

<view class="loading-state" a:if="{{loading}}">
  <text>加载中...</text>
</view>
